@model IEnumerable<Landing.Cloacking.Models.Campaign>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.WhiteLandingUrl)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BlackLandingUrl)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CloackingUrl)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WhiteLandingUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BlackLandingUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CloackingUrl)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
